These Epsilon libraries are required until the QVTr to QVTi transformation chain is recoded using QVTr.

The libraries are bundled internally to avoid problems with the packing of Java 6 builds in a Java 7 build.

See Bug 463510.
